## Docker Swarm

Docker Swarm is Docker’s built-in container orchestration system, which lets you manage multiple Docker hosts as a single virtual cluster and automatically handle container deployment, scaling, and failover.
