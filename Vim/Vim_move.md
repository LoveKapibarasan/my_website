# Normal Mode

`b` -- Backward word
`w` -- forward Word
`e` -- move End of word

`0` -- move line top
`^` -- move line top(ignoring spaces)
`$` -- move line end

`gg` -- move top of a file(go go)
`G` -- move end of file
    * `ggVG`

**Paragraph**

`{`
`}`
