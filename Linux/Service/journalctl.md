# journalctl

```
sudo journalctl
```

* `-u`=`--unit=UNIT`=systemd
* `-f` = real time
* `-t` = tag
* `-n "$N"`= length 
* `--no-pager`= Do not use pager

**`Pager`**
Next:`Space`
Quit:`q`

SYSLOG_IDENTIFIER=xxx