# SQLite

* a lightweight, self-contained database engine that stores data in a single file without needing a separate server. 
* It’s widely used in applications, browsers, and mobile devices.
* in python standard library

```bash
sqlite3 "$my_database".db
```

```bash
.schema tableName
.tables
.exit
.read filename
```
